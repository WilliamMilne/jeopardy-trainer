import React from 'react';
import styles from './EpisodeSelection.module.scss';

const EpisodeSelection: React.FC = () => (
  <div className={styles.EpisodeSelection} data-testid="EpisodeSelection">
    EpisodeSelection Component
  </div>
);

export default EpisodeSelection;
